# Pending

Canonical art for characters who are coming but are not in any written volume
yet. **Not roster entities** — `npm run refs` ignores this folder and nothing
here counts toward the `n/33` ready count.

- **Cricket** — appears soon; no specced plate yet.

When the character lands in prose, add them to `content/roster.json`, run
`npm run refs` to scaffold `CHARACTERS/<TOKEN>/`, and move the image there
under the filename that folder's README names.
